# Runtime dependencies for the DamageRace desktop client.
#
# Build the EXE with:
#   pip install -r requirements.txt
#   python build_exe.py
customtkinter>=5.2
Pillow>=10.0
pywebview>=5.0
pyinstaller>=6.0
